#include <bits/stdc++.h> // بسم  الله الرحمن الرحيم
using namespace std;
#define ll long long
struct date
{
 int month;
 int day;
 int year;
     // the string is dd/m/y
     void set_var(string s); // set  year, month and day by integar number
       bool is_valid( ); // return if the date is valid or not
int length_month(int month ); // return length of month
 int length_year(int year ); // return length of year
      bool IsLeapYear();


};
int main() {

}
